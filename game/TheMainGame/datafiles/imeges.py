def init():
    """init the veriables"""
    global imegesDict
    global img_indentation
    imegesDict={} #imeges for the game dicsenery. the keys is the path to the image file from TheMainGame\images (with / and not \)
    img_indentation={} #store the indantation of images in imegesDict (with the same key)
